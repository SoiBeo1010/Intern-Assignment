# Migrations

Place database migration files here. The assignment expects a coded conversion from the CSV into the database — consider using an ORM migration tool (TypeORM migrations, Knex, etc.).

Suggested steps:
- define `students`, `subjects`, `scores` tables
- write a migration that creates the schema
- write a seeder to ingest `dataset/diem_thi_thpt_2024.csv`
